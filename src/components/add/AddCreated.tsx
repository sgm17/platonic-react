import { AddCreatedContainer, AddCreatedText } from "./AddStyles";

export const AddCreated = () => {
    return <AddCreatedContainer>
        <AddCreatedText>the crush has been created successfully</AddCreatedText>
    </AddCreatedContainer>
}